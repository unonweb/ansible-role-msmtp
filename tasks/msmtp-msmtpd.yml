---
- tags: [msmtp-msmtpd]
  block:

  # start/enable service
  - name: Set enabled {{ msmtp_msmtpd_enabled }} on msmtpd.service
    become: true
    ansible.builtin.systemd_service:
      name: msmtpd.service
      enabled: "{{ msmtp_msmtpd_enabled }}"