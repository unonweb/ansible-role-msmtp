---
- tags: [msmtp-service]
  block:

  # start/enable service
  - name: Start/Enable msmtpd.service
    become: true
    ansible.builtin.systemd_service:
      name: msmtpd.service
      state: started
      enabled: true